{
  "mcpServers": {
    "aws-mcp": {
      "command": "bash",
      "args": [
        "-lc",
        "export AWS_PROFILE=formacaoaws AWS_REGION=us-east-1 && eval \"$(aws configure export-credentials --profile formacaoaws --format env-no-export)\" && exec uvx mcp-proxy-for-aws@latest https://aws-mcp.us-east-1.api.aws/mcp --metadata AWS_REGION=us-east-1"
      ]
    }
  }
}
